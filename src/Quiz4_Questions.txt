1. Trace Output

2. [3] Write a java method which decides if the integer number passed as a formal
    parameter is even or odd. The method signature is given, complete the method
    body.

    public static boolean even(int number) {

    }

3. [3] What is the output of the following program?
   public class TraceTest2
   {
        public static void main(String[] args) {
            int times = 3;

            System.out. println("Before the call, variable times is "
               + times);

            nPrintln("Welcome to Java!", times);

            System.out. println("After the call, variable times is" + times);
        }

        public static void PrintIn(String message, int times)
        {
           while (times > 0)
           {
           System.out. println("Local times = " + times);
           System.out. println(message);
           times--;
           }
        }
   }




